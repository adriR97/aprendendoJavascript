const [a, b, c, d] = [3, 8, 1, 27];

const soma = a + b + c + d;
const subtracao = d - a;
const multiplicacao = a * b;
const divisao = d / a;
const resto = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, resto);